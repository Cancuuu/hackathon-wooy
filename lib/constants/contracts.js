import { contractAddresses, prizePoolContracts } from '@wooy/current-pool-data'

export const CONTRACT_ADDRESSES = Object.freeze(contractAddresses)
export const PRIZE_POOL_CONTRACTS = prizePoolContracts
